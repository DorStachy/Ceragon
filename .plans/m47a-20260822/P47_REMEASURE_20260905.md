# P47 re-measurement ledger — 2026-09-05

**Supersedes, for status purposes,** `P47_REMAINING_WORK_20260902.md`, the three
`P47_LEDGER_CORRECTION_*_20260904.md` notes and `P47_WAVES_6_7_8_INTEGRATED_20260905.md`. Those
documents remain the record of *how* each state was reached; this one is the first pass since
2026-09-02 that re-derives every row from the repositories rather than carrying any forward.

Every row carries one of these states. They are never collapsed.

| State | Meaning |
|---|---|
| **MERGED** | Ancestor of the owning repository's `origin/main`, re-verified today against the fetched tip. |
| **BUILT · UNMERGED** | Committed on the repository's P47 integration branch; not on `main`. |
| **PARTIAL** | Some of the task is built and the missing part is named. |
| **BLOCKED** | Needs an owner decision, a production operation, or an external party. |
| **WITHDRAWN** | Removed from scope by the owner decision of 2026-09-04. The field the task would have filled stays expressible and says so. |
| **NOT STARTED** | No implementation exists. |
| **NOT EXERCISED** | A proof the plan requires that has not been run. |
| **MOVED** | Re-homed to another wave or programme; a headstone, not an omission. |

Nothing in this pass was pushed, merged to any `main`, or deployed.

## Measured tips — after `git fetch origin` in all eight repositories, 2026-09-05

| Repository | `origin/main` | Integration branch | Tip | Ahead | Behind |
|---|---|---|---|---:|---:|
| Installers | `51af2042` | `p47/fix-system-exfil` | `951d790a` | 133 | 29 |
| Backend | `efc70ba7` | `p47/integration-backend` | `1ff37f3f` | 27 | 2 |
| Frontend | `a9a80ae6` | `p47/w6-frontend` | `486730db` | 29 | 2 |
| GithubApp-Bot-Scanner-Worker | `c72579e` | `p47/integration-scanner` | `04309b9` | 20 | 0 |
| Sandbox-Worker | `496073f` | `p47/w7c-containment` | `0c34e95` | 2 | 0 |
| Ceragon-Intelligence | `b7e9f27` | `p47/w7c-platform` | `93c818c` | 1 | 3 |
| Static-Worker | `137f34f7` | `p47/w7b-static` | `fb8b990f` | 1 | 1 |
| docs | `7c1cc51` | `p47/w8-claim-docs` | `e26ac2f` | 2 | 2 |
| workspace root | `origin/master` | `feat/push-depth-cli-ui` | this commit | 152 | 66 |

Three mains moved since the last ledger: Installers (`48c3d2eb` → `51af2042`, 29 commits including the
daemon-resilience merge #259, which touches `internal/daemon/server.go` beside Wave 8 Task 3's edit),
Static-Worker and docs. The Installers integration branch merged onto the fetched tip with **0
conflicts** (`c4b41c58`, throwaway worktree). The rebase manifest is therefore stale by its own rule
and says so when validated; regeneration is the standing Wave −1 Task 1 procedure, not a defect.

## What this pass changed in the repositories

Four Installers branches carried P47 work that was not in the integration branch. Two were the
finished Wave 8 branches from the last session; two were older and had been missed.

| Branch | What | Now |
|---|---|---|
| `p47/w8wire` `635a2b3f` | Wave 8 T4 — the fail-open observer wired into the certificate generator | merged (fast-forward) |
| `p47/w8sinks2` `850f281b` | Wave 8 T3 — S16/S17 mediated; `Mediated` re-derived from the handlers | merged `cadd5a7b` |
| `p47/w5-t8` `ada9b9e2` + `b7e0be7c` | **Wave 5 T8 — the ASI09 confirmation-dialog fix, 830 lines, never integrated.** The dialog was showing the prompt lane's class label instead of the command being approved; the second commit closes newline forgery. Both commits were read hunk by hunk before merging; clean. | merged `cac3fccb` |
| `p47/w-1-header-truth` `0af7e06b` | Wave −1 — the two Installers workflow headers describe the triggers the files actually have | merged `c46c6e50` |

`p47/w0a-destructive-rm` is also not an ancestor, but every one of its six commits is
patch-equivalent to a commit already in the branch (`git cherry`), so it needs nothing.

Five new commits, each closing something a P47 wave left open and only a gate could see:

| Commit | Branch | What |
|---|---|---|
| `951d790a` | Installers `p47/fix-system-exfil` | Two `internal/proxy` tests the previous session filed as pre-existing were P47's own (bisected to `2eaf0f21`, Wave 4A T2); the proxy lane's expectations now match the self-vocabulary decision. |
| `8834896d` | Backend `p47/integration-backend` | Wave 6 T9 and Wave 8 T2 both took migration timestamp `1793400000000`; the Wave 8 migration moves to `1793500000000`. Caught by the mirrored `pr-checks:lint` leg. |
| `04309b9` | GithubApp-Bot-Scanner-Worker `p47/integration-scanner` | Wave 7A T7's Postgres integration spec refuses to skip under CI and `test.yml` gave it no Postgres, so the scanner-worker jest job could not have passed on GitHub. The job now carries the same `postgres:17` service Backend uses; the leg passes in the mirror. |
| `042a74a` | workspace root `ci/lib/run.mjs` | Two opt-in knobs, `DEVOIDCI_CPUSET` and `DEVOIDCI_MEMORY`, after a container jest leg with every core wedged the Docker daemon. Off unless set. |
| `1ff37f3f` | Backend `p47/integration-backend` | Wave 6 T8/T9 widened `ai-governance-contract.d.ts` (43,695 → 48,514 bytes) without refreshing the committed-dist pin, so every Backend leg that runs the consumer check first failed before its first test. Refreshed through the documented `--refresh-committed-dist-pin` path. |

At the workspace root the plan's five mentions of the ATLAS release `v2026.07` now read `2026.08`,
which is what `Installers/internal/certificate/standards.go` pins and documents (2026.08 released
2026-08-31; purely additive over 2026.07). The claim-contract guard was re-run on the edited plan:
15 checklist rows, 15 renderer entries, PASS.

## Wave −1 — rebase, authority regeneration, claim contract (8 tasks)

| Task | State | Evidence |
|---|---|---|
| T1 Fetch first, publish a rebase manifest, make it standing | **BUILT · UNMERGED** | root `1cec913`; `ci/lib/rebase-manifest.mjs`. Self-test `w-1-rebase-manifest.test.mjs`: 15 of 15 on a quiet run; 1 of 15 failed once while the Go suite was running beside it — load-sensitive, not wrong. Validator run today reports the manifest **stale** (Ceragon-Intelligence 3 behind, Static-Worker main moved), which is the instrument working. |
| T2 Write the claim contract into the goal statement | **BUILT · UNMERGED** | root `cdaab5a`, `4635386`, `23019ea`, `116945b`. 15 fenced checklist rows; `ci/lib/claim-contract.mjs` self-test 41 of 41. Equality measured against the integration renderer: **15 = 15, PASS.** Against the live `Installers/` checkout the renderer is absent and the guard exits 2, NOT MEASURED, as designed. Seven P47 documents (plan, four landed notes, claim template, system card) scanned: no forbidden claim. |
| T3 Resolve provenance of the pinned DLP vocabulary — discovery | **MERGED** | Unchanged from the 09-02 ledger; no runtime source involved. |
| T4 Repair every citation in the plan | NOT STARTED | No citation repair exists. The rebase manifest states the resolution rule (`git show origin/main:<path>`) but the drifted line numbers themselves stand. |
| T5 `holdout-score.yml` header truth, then the trigger decision | **PARTIAL** | The header half is done: the file now says it runs nightly and on manual dispatch, never on a push or pull request, and `on:` is exactly `workflow_dispatch` + `schedule`. The 09-02 ledger measured the old header claiming a push trigger. The trigger decision itself stays **BLOCKED** on the owner's cost call. |
| T6 Declare the standards columns in the manifest schema | **BUILT · UNMERGED** | Delivered inside Wave 8 T6/T7: `schema.json` carries `standardsMapping` under `additionalProperties: false`; `internal/certificate/standards.go` is the mapping. |
| T7 Create the `toolrisk-lane` job and its mirror entry | **BUILT · UNMERGED** | root `353b173`; `ci/gates.json` `pr-checks:toolrisk-lane`. `drift.mjs` today: no drift across all seven repositories. |
| T8 Give the pinned-artifact guard a separately-runnable leg | **BUILT · UNMERGED** | Backend integration `pr-checks.yml:736` job `shared-contracts-pin`; `ci/gates.json` `pr-checks:shared-contracts-pin`. The 09-02 ledger had this NOT STARTED. |

## Wave 0A — stop hard-blocking ordinary work (6 tasks)

| Task | State | Evidence |
|---|---|---|
| T1 Watch the block on a live endpoint before changing anything | **NOT EXERCISED — permanently** | Unchanged; cannot be recreated after the fact. |
| T2 Twin tables as a failing test | **MERGED** | `internal/toolrisk/home_target_boundary_test.go` on `origin/main`. |
| T3 Narrow the two POSIX home alternatives | **MERGED** | Same chain. The uncommitted 2026-08-29 reconciliation block in `w-minus-1_w0.md` records the implemented boundary (50-row matrix, 18 removals + 1 addition). |
| T4 Benign shapes into the ordinary-work corpus | **MERGED** | `W0A_COVERAGE_DELTA.md` on `origin/main`. |
| T5 Prove no Backend deploy is needed, then cut the agent release | **BLOCKED** | Owner authorization. Live customer impact continues while open. |
| T6 Confirm on the live endpoint after, publish the delta | **BLOCKED** | Depends on T5. |

## Wave 0 — emergency egress correction (8 tasks)

| Task | State | Evidence |
|---|---|---|
| T1 Strip both `ALLOW_MINIMAL` overrides from the live task definitions | **BLOCKED** | Production mutation. |
| T2 Task-def validator privacy/egress invariant | **MERGED** | `deployment/validate-taskdef-security.js` + test on Scanner `origin/main`: rejects an enabled escape hatch, accepts absent or explicitly false. |
| T3 Remove the overrides from the committed task-defs | **MERGED** | The only `ALLOW_MINIMAL` mentions left under `deployment/` are the validator and its test. |
| T4 Enable ECS Exec on `backend-service` | **BLOCKED** | Production IAM/service mutation. |
| T5 Restore depth on our own repositories via `.codefence.yml` | NOT STARTED | No `.codefence.yml` on any of the seven integration branches. |
| T6 Restore depth on the local-scan lane | NOT STARTED | |
| T7 Deferred live verification at power-on | **NOT EXERCISED** | Scanner services 0/0; Docker Desktop is down on this machine today, so the local mirror could not stand in either. |
| T8 Residual pre-egress boundary and the R1 certificate row | **MERGED** | Scanner #44. R1 stays `NOT_READY` with named blockers. |

## Wave 1 — policy authority and catalog totality (7 tasks)

**All seven MERGED**, re-verified on the fetched `origin/main` of Backend and Frontend today:
`packages/shared-contracts/dlp-classes.v1.json` present; `AI_SECURITY_DLP_CLASSES` counts **81**;
`dlp-governance-gap.ts` present; parity specs present on both sides; the three malicious-floor specs
present. T2 remains "attribution inferred" — the delivered state exists, no commit names it. Exit
evidence has still not been run as a set.

## Wave 2 — evidence strength, consequence, UI vocabulary (10 tasks)

**All ten MERGED**, re-verified today: the five-band migration
`WidenAiEventSeverityToFiveBands` (2 files), `ai-event-severity.util.ts`, and the Frontend type
`AiEventSeverity = "info" | "low" | "medium" | "high" | "critical"`. **T2 is still NOT DEPLOYED**, and
O-6 makes that ordering load-bearing for the console. The later self-defense-floor correction that
the uncommitted `w2_evidence_severity.md` addendum asks for is implemented:
`TestSelfDefenseFloorDoesNotRelaxUnspecifiedHighFindings` is on Installers `origin/main`.

## Wave 3 — measurement substrate (11 tasks)

| Task | State | Evidence |
|---|---|---|
| T1–T3 Scorer test file, per-class denominators, UNKNOWN-not-zero | **MERGED** | `cmd/ai-security-neutral/holdout_test.go` and `internal/neutraleval/holdout_seal_test.go` on `origin/main`; 10 neutraleval test files there. |
| T4 Mandatory engine version | **MOVED** → 3B T1 | |
| T5 System-under-test tuple | **MOVED** → 3B T2 | |
| T6–T10 Inspection budget, per-class lane record, four lane seams, deterministic report, shared invalidation | **MERGED** | Unchanged from 09-02; `InvalidationResult` referenced from two files on `origin/main`. |
| T11 Trigger-cost decision | **MOVED** → Wave −1 T5 | Owner-blocked there. |


## Wave 3B — evaluation and corpus governance (13 tasks)

Every row below is on `p47/fix-system-exfil` unless it says Backend. Three of the suites this wave
built are **red by design** in the full Go run, and each red says in its own failure text what is
unmet and forbids relaxing the gate; they are listed under the suite table so nobody re-diagnoses them.

| Task | State | Evidence |
|---|---|---|
| T1 Engine version mandatory, stamped on the aggregate | **BUILT · UNMERGED** | three `w3b-t1` commits; `m4.7` survives only as a value the runner refuses. |
| T2 System-under-test tuple | **PARTIAL** | 11 axes emitted on `system`; `RunnerIdentity.Valid()` deliberately not widened (three production callers persist five-field identities). Moves when the D-3B-6 spine change lands. Recorded, not silent. |
| T3 Emit the evaluation report | **BUILT · UNMERGED** | `clusteringUnit`, `nEffective`, `rho` emitted. |
| T4 Suite registry, six suites | **BUILT · UNMERGED** | `parity-vectors/neutral/suite-registry.json`: 6 suites, 14 corpora, 4 Tier-A classes of a ceiling of 6; `claimSupported` derived, not typed. |
| T5 Suite 1 — canonical regression set | **BUILT · UNMERGED** | Gate `TestCanonicalRegressionSetIsComplete` is **red by design**: five canonical residuals open. |
| T6 Suite 2 — transform inventory as a gate | **BUILT · UNMERGED** | Fails on an undeclared NOT-CAUGHT; three owned residuals. |
| T7 Suite 3 — benign-replay denominator | **PARTIAL** | Engineering half built (a bound cannot publish below its own denominator, 123/133). The data-collection programme itself is **BLOCKED (E1)** — it always was the blocked half. |
| T8 Suite 4 — e2e environments | **BUILT · UNMERGED** | `e2e_scenarios.go`, scenario index, `DesignEffect` / `EffectiveScenarioSize`. |
| T9 Suite 5 — private adaptive holdout | **BUILT · UNMERGED**, arm **WITHDRAWN** | `AdaptiveArm` on every `evaluation`; `adaptiveAsr: null`; charter written. The arm itself was withdrawn by the owner 2026-09-04. |
| T10 Suite 6 — incident suite | **BUILT · UNMERGED** | Gate `TestZeroUnmigratedIncidents` is **red by design**: 3 of 26 incidents unmigrated. |
| T11 Labeler and adjudicator governance | **BUILT · UNMERGED** | Gate `TestEnforcingCasesHaveTwoLabelers` is **red by design**: 0 of 404 stratum-A cases carry two labelers; the missing thing is a second independent human (E4, **WITHDRAWN**). Reliability reports `null`. |
| T12 Reconcile the two vocabularies | **BUILT · UNMERGED** (Backend) | `w3b-t12` is on `p47/integration-backend`. The 09-04 note called it stranded; it is not any more. |
| T13 Generators into the repository | **BUILT · UNMERGED** | The two corpus generators prove the committed corpus is theirs. |

## Wave 4A — close the published residuals (8 tasks)

| Task | State | Evidence |
|---|---|---|
| T1 `qa-fp-migration-timestamps` | **BUILT · UNMERGED** | `financial.go` header records the Tier-B PAN move. |
| T2 `qa-fp-detections-finding-name` | **PARTIAL** | Go: `selfvocab.go` (`2eaf0f21`) plus today's `951d790a`, the proxy lane's parity-vector expectations that the first commit missed. Sealed promptrisk benign interruptions 1 of 6 → 0 of 6. **Browser twin: only the policy-eval half was ported** (`aa69edd9`, `policyeval.js`); `browser-extension/src/promptrisk.js` has no self-vocabulary suppression. On the shared §3.4 corpus as it stands today the browser engine fires on **24 of 91** benign cases where Go fires on **15 of 92**, and the JS-only set is dominated by the product's own vocabulary. The cross-engine gate (`go-corpus-cross-engine.test.mjs`, run by Installers `pr-checks:scanner-parity`) is red for that reason and was left red rather than re-pinned. |
| T3 `attack-system-prompt-exfil` | **BUILT · UNMERGED** | Sealed promptrisk lane 5 of 5. |
| T4 `attack-prod-db-connection-string` | **BUILT · UNMERGED, with a recorded discrepancy** | The false negative is closed; the finding lands at Tier C, not the Tier A the task asks for, and `W4A_T4_FIXTURE_DEFECT_20260904.md` says why the Tier-A expectation is unmeetable as written. |
| T5 `attack-private-key-block` reporting gap | **PARTIAL / BLOCKED** | The reporting seam is built (`w4a-t5`); the posture is an owner decision. Still the only miss on the sealed dlp lane. |
| T6 Ingress private-key redactor seam | **PARTIAL / BLOCKED** | Seam built (`w4a-t6`: the key is seen, and the reason it is still forwarded is recorded); same owner decision. |
| T7 A benign hard block is never bankable | **BUILT · UNMERGED** | `ToolRiskFPHardBlockRefusal`. |
| T8 Canonical registry populated; packages in the lane | **BUILT · UNMERGED** | 11 members with the insufficiency clause; `pr-checks.yml` runs `policyeval`, `dlp`, `promptrisk`. |

## Wave 4B — tool/effect detection quality (10 tasks)

**All ten BUILT · UNMERGED.** T1 and T8 span Installers, Backend and Frontend (`vocab-parity.mjs`
passes across all three; 29 self-test cases today). `git grep -c deriveCombos -- internal/toolrisk`
is 0. T10 is the `W4B_WINDOWS_NOT_READY.json` record, as the plan rewrote the criterion.

## Wave 4C — prompt/ingress detection quality (11 tasks)

Closure record: `v2-waves/W4C_CLOSURE_20260903.md` (four measurements re-derived by an independent
verifier; nine inert shapes found and closed).

| Task | State | Evidence |
|---|---|---|
| T1–T6, T8, T9 | **BUILT · UNMERGED** | Tagged commits on `p47/fix-system-exfil`; the over-defence corpus reads 5 of 36 egress, 7 of 16 ingress. |
| T7 Gate the six-class promotion, and let it stay shut | **BUILT · UNMERGED** | `scripts/prompt-promotion-gate.mjs` + defeat suite (`4bf0d633`, `adcf912d`, `a144359a`, `e379de31`). Prints HOLD for all six. |
| T10 Commission the adaptive arm | **WITHDRAWN** | Owner decision 2026-09-04. `adaptiveAsr: null`, 0 scenarios against a target of 1,744. |
| T11 Codex surface safeguards-on | **WITHDRAWN** | Codex vendor artefacts withdrawn; the dialect pin is not widened on inference. The safeguards-off column is what is published, and it says which it is. |

## Wave 5 — console truth (11 tasks)

Closure record: `v2-waves/W5_CLOSURE_20260904.md` (eight defects in the wave's own first build, all
fixed and re-banked).

| Task | State | Evidence |
|---|---|---|
| T1 Render harness as the gate | **BUILT · UNMERGED** | four `w5-t1` commits; the banked run and the CI decision recorded at the root. |
| T2 A failed AI-plane read stops reading as an empty fleet | **BUILT · UNMERGED** | Frontend `8353141e`. |
| T3 MCP queue stops asserting a count it cannot see | **BUILT · UNMERGED** | `w5-t3`. |
| T4 Two green zeros that describe a failure | **BUILT · UNMERGED** | three `w5-t4` commits. |
| T5 Sort, tab counts, filter note | **BUILT · UNMERGED** | `w5-t5`. |
| T6 Endpoint's own word not drawn in the success token | **MOVED** | `coverage-section.tsx` is assigned to the other programme. |
| T7 D14 — a daemon 401 means NOT GOVERNED | **MOVED** | `agent_shim.go` is assigned to the other programme. |
| T8 Confirmation dialog names the action (ASI09) | **BUILT · UNMERGED — integrated today** | Built 2026-09-03/04 on `p47/w5-t8` and never merged; `cac3fccb` today. `go test ./cmd/devoid -run 'Warn|Dialog|Forgery'` ok on the merged tree. |
| T9 Vendored-engine drift check nothing can switch off | **BUILT · UNMERGED** | root `d19d624`, `69db20c`; `vendored-engine-parity.test.mjs` all cases today. |
| T10 Certificate manifest projected onto the console | **BUILT · UNMERGED** | Frontend `486730db`: `types/certificate.ts`, `certificate-panel.tsx`, 10 tests; `null` renders NOT MEASURED, expiry outranks status. |
| T11 Lane headers not the only answer to "is anything set to warn?" | **BUILT · UNMERGED** | `w5-t11`. |

## Wave 6 — triage, adjudication, incident learning (13 tasks)

**All thirteen BUILT · UNMERGED**, split Backend (T4, T8, T9, T10, T11, T12, T13) and Frontend
(T1, T2, T3, T5, T6, T7), every commit tagged. Backend evidence by file: T8 widens the taxonomy in
`ai-governance-contract.ts` and adds `TRIAGE_GOVERNANCE_MAPPING.md`; T9 adds 300 lines to
`ai-event-triage.service.ts` so a second reviewer cannot be overwritten; T12 is
`ai-policy-rollback-storm.service.ts`; T13 is `FP_AGENT_BOUNDARY.md` plus a lane boundary in the
triage service. **T8 edits Backend's copy of `@ceragon/shared-contracts`; the root and
Ceragon-Intelligence copies are not updated.** `P47_MIRROR_DIVERGENCE_20260905.md` records that the
mirror guard has never run here; the Backend suite result below is where that lands.

## Wave 7A — scanner execution truth (8 tasks)

| Task | State | Evidence |
|---|---|---|
| T1–T4, T6 | **BUILT · UNMERGED** | `p47/integration-scanner`; execution-manifest, scan-exit-decision and upload-runtime-envelope suites 40 of 40 at landing. |
| T7 Stop the worker erasing the fail-closed stamp | **BUILT · UNMERGED** | Its Postgres integration spec refuses to skip under CI, which is right, and `test.yml` supplied no Postgres, which meant the scanner-worker jest job could not have gone green on GitHub. Found by the mirror, closed by `04309b9`; the leg passes with the service. |
| T5 Wire `main.ts`; composite outputs real | **PARTIAL** | The D3 gate needs one real fork-PR run before a release tag, and that needs a push. |
| T8 A COVERAGE_FAILED scan tells the developer why | **BUILT · UNMERGED** (Backend) | `results.controller.ts`, `f83ec0e5`. |

## Wave 7B — scanner detection certification (9 tasks)

| Task | State | Evidence |
|---|---|---|
| T1, T3, T4, T5 | **BUILT · UNMERGED** | Evidence manifest; strata; twins, mutation, repair-revert; per-engine attribution and pins. |
| T2 Restore the push legs; arm the recall benchmark | **PARTIAL** | The benchmark cannot run here: live LLM keys, a Linux Joern/SCIP toolchain, spend. `baselineFpCapture: NOT_MEASURED`; writing 0 reddens a test. |
| T6 Sealed scanner-injection corpus, behavioural | **PARTIAL** | Graded against the real `wrapUntrusted`; 0 of 4 LLM routes executed because the route artefacts are the withdrawn Codex material. |
| T7 Prove the signing lane live | **PARTIAL** | 7 of 13 bypasses closed, all body-tamper; the six open (replay, expiry, wrong queue, wrong producer, page completeness, KMS-unverifiable) carry written gaps, and a correctly signed message is asserted accepted so a reject-everything verifier cannot pass. |
| T8 Static-Worker P1-04 | **BUILT · UNMERGED, one owner decision open** | Static-Worker `fb8b990f`: the package and artifact gates now ask per applicable ecosystem and class, and the catch rate is a measured 122 of 123 pairs instead of 1 by construction. Asking the right question found a hole — a zero-width directive is caught as a skill, MCP server and editor extension and goes ALLOW as a plugin bundle — and the commit **re-banked that escape into `corpus/artifact-fixtures/CATCH_BASELINE.json`**, a file the repository's own forbidden-path guard marks immutable because banking an escape is "a human decision" (`node corpus/campaign.cjs catch --write-baseline`). The commit says so itself and leaves the guard red: veto-gates 28 of 29. Re-measured today on a complete install: 204 suites pass, that one fails with exit 3 = `IMMUTABLE_FILE_CHANGED`. The owner either accepts the re-bank through the campaign command or the change is reworked. |
| T9 R2 certificate row | **BUILT · UNMERGED** | `r2-certificate.ts`; ATLAS row corrected to 2026.08 (`57e30e3`). R2 stays `NOT_READY` on branch protection, which is a billing plan, not code. |

## Wave 7C — sandbox containment (2 tasks)

**Both BUILT · UNMERGED.** T1 on Sandbox-Worker and Ceragon-Intelligence (both platform-routing
guards confirmed and pinned); T2 on Sandbox-Worker (npm, PyPI and Go packages the sandbox cannot
contain are no longer executed; 22 of 22 containment-gate tests at landing). The deploy half of T2 is
owner-gated.

## Wave 8 — authoritative enforcement, canary, rollback, certificate (12 tasks)

| Task | State | Evidence |
|---|---|---|
| T1 Sink inventory as a gate | **BUILT · UNMERGED** | 20 sinks; `sink-inventory.json` regenerated. |
| T2 Widen the binding to the P0-16 field set | **BUILT · UNMERGED** | Installers: five NUL-separated preimage segments with a distinct ABSENT marker (the plan's literal text would have collapsed an ungoverned command onto the sentinel and widened a grant; it was not followed). Backend `d8f17bb5`: effect binding on the delegated-approval tuple plus its migration. |
| T3 Route every effect-permitting sink through the checkpoint | **PARTIAL** | 6 of 20 authoritative, 1 taint-overlay, **13 residuals**, each with an owner and a reason. S2 prompt egress is deferred on purpose: a synchronous authority round-trip on the prompt hot path meets the 4-second hook budget, and a hook that expires mid-decision turns WARN into PROCEED. `Mediated` is now re-derived from the handlers in both directions. |
| T4 Fail-open forces a non-green certificate (D14) | **BUILT · UNMERGED** | Installers `failopen.go` wired into the generator (defeat: five tests red); Backend `417cc1a0` makes the fragment manifest-valid. |
| T5 Adopt P9's per-call-site I/O grace; keep the live-proof gate | **MERGED** (engineering) / **NOT EXERCISED** (live) | `internal/aicanary/exec_iograce_test.go` is on `origin/main` and the package is ok in every run today. The 6-of-6 live canary run is owner-gated. |
| T6 Certificate manifest — schema, generator, two-tier gate | **BUILT · UNMERGED** | `schema.json` repaired (`a01788cc`: the precision object it prescribed was unsatisfiable, and `notMeasured` forced a fabricated zero — both mine); generator; `cmd/devoid-certificate`. |
| T7 Class catalog mapped to external control ids | **BUILT · UNMERGED** | `standards.go`, ATLAS pinned at 2026.08 with the diff recorded. |
| T8 Rings, automatic halt, rollback | **BUILT · UNMERGED** (Backend) | `ai-policy-halt.service.ts`, 850 lines, `3ebce68e`. |
| T9 Live effect canary, secret-egress canary, independent reproduction | **BLOCKED** | Live evidence; the independent reproducer is withdrawn scope. |
| T10 F16 signing-key custody | **BLOCKED** | Owner/infrastructure decision, unchanged. |
| T11 Claim template and system card | **PARTIAL** | Docs built on `p47/w8-claim-docs` (two commits); the renderer `claim_test.go` matches the 15 rows. `TestSystemCardHasAPerSurfaceRow` does not exist on any branch, so the card's per-surface rows are not yet bound to code. |
| T12 §16.8/§16.9 defeat matrix as executable tests | **PARTIAL** | Backend `9b1fa548`: the 27-row matrix is a ledger that says how many rows run. No Go test names the matrix; the Installers rows are not written. |


## Suite results — one machine, one run at a time

Every command below ran on this laptop with nothing else running beside it, except where a row
says otherwise. "Alone" means the failing test re-run by itself; "main" means the same command on a
throwaway worktree at the fetched `origin/main` with the same `node_modules`.

| Repository · tree | Command | Result | What the reds are |
|---|---|---|---|
| Installers · merged tree `c4b41c58` (`51af2042` + `951d790a`) | `go test ./... -count=1` | **149 packages ok**, 2 not | `internal/neutraleval`: three gates **red by design** — `TestCanonicalRegressionSetIsComplete` (five canonical residuals), `TestZeroUnmigratedIncidents` (3 of 26), `TestEnforcingCasesHaveTwoLabelers` (0 of 404 doubly labelled; E4). `internal/skillgate` `TestResolve_PluginFastPathHonoursContext`: a nanosecond-deadline test, green alone, untouched by P47. |
| Installers · plain `origin/main` `51af2042` | same | 147 ok, 1 not | `TestEndTrackedAISessions_BoundedFanOutCompletesInTimeBox` fails under full-suite load **on main too**; green alone on every tree. neutraleval is green on main because the three red gates are P47's. |
| Installers · integration before the fix | same | 147 ok, 4 not | The two extra: `internal/proxy` `TestRedactIngressText_BenignCorpusUntouched` and `…KeywordTiersAreGradedPerRuleNotFromTheCatalog`. Green at the merge-base `48c3d2eb` and on main; `git bisect run` → first bad commit `2eaf0f21` (Wave 4A T2). Not pre-existing. Fixed in `951d790a`; the corrected vector row goes red again when the suppression is removed. |
| Backend · `p47/integration-backend` `417cc1a0` | `jest --ci --maxWorkers=50%` | **1026 suites passed, 86 skipped, 1 failed**; 17,435 tests passed, 1 failed | `fastgate.service.spec.ts`, a prescan-bulk *time-budget* assertion. Alone: 87 of 87 on the integration branch **and** on main `efc70ba7`. The 86 skipped are the live-Postgres specs, which gate on `DATABASE_*` and run in the gate mirror's live-Postgres legs. An earlier attempt at 12 workers produced 10 failures, all five-second timeouts, one two-second performance bound and one Docker-dependent build-stamp gate while Docker was down; none reproduced at six workers. |
| Frontend · `p47/w6-frontend` `486730db` | `jest --ci --maxWorkers=50%` | **619 of 619 suites**; 7,668 tests, 14 skipped | — |
| Frontend | `tsc --noEmit`; `check:contrast` | both clean | — |
| Frontend | `npm run lint` | 20 errors, 484 warnings | **The same 20 errors, in the same ten files, on main** (20 errors, 468 warnings at `a9a80ae6`): six conditional hooks in `endpoint-hub-content.tsx` and one in `agents-content.tsx` that predate P47, twelve "definition for rule not found" config-drift errors, one `<a href="/">`. P47 adds no error and 16 warnings. Lint is not one of Frontend's four PR gates, which is why nothing upstream ever went red for it. |
| GithubApp-Bot-Scanner-Worker · `github-action` | `jest --runInBand --ci` | **65 of 65 suites**; 792 tests | — |
| GithubApp-Bot-Scanner-Worker · `scanner-worker` | `jest` (default config) | 151 passed, 2 skipped, **1 failed** | `opus-cost-governed-routing.spec.ts`, 2 of 5 cases: `ENOENT` on a temp-workspace path. Fails alone, and **fails identically on `origin/main` `c72579e`** in a fresh worktree. Untouched by P47; a Windows-host result the Linux gate mirror re-measures below. |
| GithubApp-Bot-Scanner-Worker · `scanner-worker` | `jest` (phase-6 config) | **2 of 2 suites**; 88 tests | — |
| Sandbox-Worker · `p47/w7c-containment` | `jest` | 77 passed, **14 failed to run** | Every one is `Cannot find module '@iarna/toml'` (26 sites) or `chalk`. Both are declared in `package.json` on main and on the branch; the local `node_modules` this worktree shares with the live checkout was installed before they were added. **A stale local install, not a code result.** Giving the worktree its own install was tried and **the DeVoid shim blocked it**: `COVERAGE_FAILED — endpoint-lite cannot govern dependency source types: FILE_PROTOCOL=1` (the package has a `file:` dependency). That is the product failing closed on its own machine, and it was not bypassed. Host result for Sandbox-Worker: **NOT MEASURED**; the Docker mirror, which installs inside Linux, is the instrument — see the gate section. |
| Static-Worker · `p47/w7b-static` | `jest` | 202 passed, **3 failed to run**, 1 assertion | `TS2307 Cannot find module '@aws-sdk/client-kms'` — declared on main and on the branch, present in the live checkout's `node_modules`, absent from this worktree's stale copy. **Re-run with the complete install: 204 suites pass, 1 fails.** The one is `veto-gates.test.ts` › the forbidden-path guard, exit 3 = `IMMUTABLE_FILE_CHANGED corpus/artifact-fixtures/CATCH_BASELINE.json`: Wave 7B T8 re-banked an escape into the immutable recall baseline, which the guard reserves for a human. The commit says so; see Wave 7B T8. Not an environment result. |
| Ceragon-Intelligence · `p47/w7c-platform` | `jest` | 47 passed, **60 failed to run**; 108 test failures | 276 × `TS2305 Module '@ceragon/shared-contracts' has no exported member …`. The vendored mirror's source defines every missing member on the branch and on main; what the tests resolve is `node_modules/@ceragon/shared-contracts`, a copied install whose `dist/` predates them. `npm ci` refreshes that copy, which is what CI does and this host had not. **Stale local install.** The branch's one commit is a comment. A worktree-local `npm ci` was tried twice and **the DeVoid shim blocked it both times** — first "dependencies could not be verified in time" at 20 of 431 resolved, then "backend local dependency scan failed: resolved 0 of 0 packages within 45s". Fail-closed, not bypassed. Host result: **NOT MEASURED**; the Docker mirror installs inside Linux and is the instrument. Worth its own note: on the owner's own machine, the shim refused two ordinary installs in one afternoon, for a `file:` dependency and for a slow Backend scan. |
| Ceragon-Intelligence | `node --test scripts/lib/*.test.mjs` | clean | — |
| workspace root | `claim-contract.test.mjs` | 41 of 41 | — |
| workspace root | `claim-contract.mjs` against the integration renderer | **15 = 15, PASS**; seven P47 documents carry no forbidden claim | Against the live `Installers/` checkout the renderer is absent → exit 2, NOT MEASURED, by design. |
| workspace root | `vocab-parity.test.mjs`; `vendored-engine-parity.test.mjs` | 29 cases; all cases | — |
| workspace root | `w-1-rebase-manifest.test.mjs` | 15 of 15 quiet | 1 of 15 failed once while the Go suite ran beside it. |
| workspace root | `rebase-manifest.mjs` (validate) | **stale**, exit 1 | Ceragon-Intelligence 3 behind, Static-Worker main moved. This is the manifest doing its job; regenerate with `--write` after the seven-repo fetch, per Wave −1 T1. |
| workspace root | `drift.mjs` | no drift, all seven repositories | — |

### The GitHub gate mirror, in Docker, on the tree a pull request would build

The runner resolves each repository by path from the workspace root and would have read the live
checkouts, which are on the other programme's branches. It ran instead from a junction shadow of
the workspace pointing at the eight P47 worktrees (`node --preserve-symlinks --preserve-symlinks-main
ci/lib/run.mjs <repo> --merged --jobs 1`), so every leg below is the merged tree of the integration
branch and the fetched `origin/main`, and no live checkout was switched. It took two passes. The
first ran Installers in full and Backend before this pass's two Backend fixes, then Frontend's jest
leg ran 35 minutes inside a container with every core, wedged the Docker daemon for the rest of the
hour, and had to be stopped. The second pass, after a Docker restart, ran each container capped to
six cores (`DEVOIDCI_CPUSET=0-5`, a new opt-in knob in `ci/lib/run.mjs`, commit `042a74a`) with a
wall-clock cap per leg; no leg tripped a cap.

**Installers — 14 legs: 9 pass, 5 fail.** Every failure is placed.

| Leg | Result | Why |
|---|---|---|
| `pr-checks:codex-hook-lane-live-proof` | fail, **by design** | Two Codex builds (0.134.0, 0.146.0-alpha.3.1) have no live observation in the ledger; the gate forbids closing that any other way. Owner-gated. |
| `pr-checks:wire-lane-tests` | fail, **by design** | `TestEnforcingCasesHaveTwoLabelers`, the Wave 3B T11 gate (E4). |
| `pr-checks:scanner-parity` | **fail, P47's** | 1,284 of 1,289 browser-parity tests pass. Four are manifest-coverage assertions that fail identically on plain `origin/main`. The fifth is ours: the browser engine fires on 24 of 91 benign corpus cases where Go fires on 15 of 92, because Wave 4A T2's suppression was ported to the twin's policy-eval and not its prompt scanner. Left red. |
| `pr-checks:codex-vendor-lane` | fail, **mirror** | The pinned Codex CLI installs, but the canary refuses the container's binary as `canary-host-executable-untrusted`. |
| `holdout-score:score` | fail, **mirror** | The runner cannot resolve `steps.engine.outputs.version`, skips the four scoring steps, and "Publish the rates" finds nothing. NOT MEASURED by the mirror. |

Passing: `finding-b-e2e:shim-enforcement`, `ai-checkpoint-observation`, `hard-deny-stress`,
`hot-path-audit-imports`, `macos-legacy-identity`, `release-workflow-contract`, `self-update-lane`,
`toolrisk-lane` (the Wave −1 T7 leg), `uninstall-honesty`.

**Backend — 16 legs, run twice.** First pass on `417cc1a0`: 4 pass, 2 partial, 10 fail, and every
failure had one of two causes: nine legs hit the un-refreshed committed-dist pin
(`audit_integration`, `alerts_integration`, the four `full_test` shards, `build_and_test`,
`e2e_advisory`, and `shared-contracts-pin` itself, the Wave −1 T8 leg doing exactly what it was
added for), one hit the duplicate migration timestamp (`lint`). Second pass on `1ff37f3f`:

| Leg | Second pass |
|---|---|
| `lint`, `shared-contracts-pin`, `audit_integration`, `alerts_integration`, `e2e_advisory` | **pass** |
| `full_test` shards 2, 3, 4 | **pass** |
| `build_and_test` | 1,111 of 1,112 suites, 18,713 tests, **zero test failures**; the one suite that could not run is `build-stamp-deploy-gate.spec.ts`, which builds a Docker image, and the mirror's Backend legs mount no Docker socket. P47 never touched it; it passes on the host with Docker up. |
| `full_test` shard 1 | 277 of 278 suites, 4,828 tests, zero test failures; the same Docker-building suite sits in this shard. |
| `typecheck`, `licenses_integration`, `migration_chain_from_empty`, `security:audit` | pass (first pass; unchanged) |
| `security:rate_limit_taskdef`, `security:rds_boundary` | partial, as `gates.json` declares: offline self-tests pass, then the cloud-credential fence. |

**Frontend — 5 legs.** `Typecheck` pass (81 s). `No customer-visible em dashes` pass. `security:audit`
pass. `Detector vocabulary parity (Installers -> Frontend)` **NOT MEASURED**: it reads Installers
over the network with a GitHub token and refuses to pass without comparing anything; the
workspace-level `vocab-parity.mjs`, which compares the three local copies, passes and is the standing
evidence. `Tests (jest)` **not completed in the mirror** (it is the leg that wedged the daemon and was
not re-run); the host run, 619 of 619 suites, stands.

**GithubApp-Bot-Scanner-Worker — 14 legs: 8 pass, 6 fail.** Passing: `ai-detection-gate`, the
`security:audit` legs for scanner-worker and shared-schemas, `github-action-clean-install`, the
shared-schemas tests, and all three typechecks. The six:

| Leg | Why |
|---|---|
| `ai-vs-scanner-benchmark:benchmark`, `security:rule-precision` | "regenerated semgrep artifacts not committed". The regenerated files differ from the committed ones **only in line endings**: the Windows working copy is CRLF, the container writes LF, and plain `origin/main` regenerates identically. A mirror artifact. |
| `quality-precision-gate` | `sudo: command not found` installing gitleaks. Mirror image. |
| `security:audit#package=github-action` | One high advisory, `browserslist`, in a lockfile byte-identical to `origin/main`. The advisory arrived after the lockfile; an upstream dependency bump, not P47's. |
| `test:test#package=github-action` | 751 of 756 tests; the four that fail shell out to `git` and the volume copy has no repository. Mirror. Host: 65 of 65 suites. |
| `test:test#package=scanner-worker` | 154 of 155 suites; the one is **P47's**. Wave 7A T7 added a Postgres integration spec that refuses to skip under CI ("a missing database is a configuration failure") and `test.yml` supplied no Postgres, so the job could not have passed on GitHub either. Fixed by giving the `test` job the same `postgres:17` service Backend's workflow uses (`04309b9`); the leg re-run in the mirror with the service started **passes**, 77 s. |

**Ceragon-Intelligence — 7 legs: 4 pass, 3 fail.** `validate:validate` ran the whole suite inside
Linux: **112 suites, 1,737 tests, all green**, which is the number the host could not give; it then
failed at "Build container image" because the container has no `docker`. `compose-config` needs
`sudo`. `cross-reference-aws-baseline` reports the sandbox compose image ahead of the AWS baseline
snapshot; the three upstream hot-set commits moved it and P47 touched neither file. `actionlint`,
`env-template-lint`, `iam-policy-lint`, `shellcheck` pass.

**Sandbox-Worker — 1 leg, partial by design, and green where it counts.** Install, both audits,
build, **Test**, the task-definition policy gate and the worker-result contract gate all pass; the leg
then stops at the cloud-credential fence before the deploy steps. Sandbox has its number.

**Static-Worker — 1 leg, not measured by the mirror.** The install succeeds, then "Verify
pnpm-lock.yaml unchanged" dies with `not a git repository`: the volume copy carries the worktree's
`.git` pointer file with a Windows path. The tests never start. The host run on a complete install
stands: 204 suites pass, one fails on the forbidden-path guard for the reason Wave 7B T8 records.

**Gaps in the mirror itself, found today and not P47's to close:** Backend legs need the Docker
socket for the build-stamp spec; the images lack `sudo`; CRLF working copies reach Linux volumes;
a worktree's `.git` pointer file is copied as if it were a repository; network legs need a token;
container jest was uncapped until `DEVOIDCI_CPUSET`.

Not run anywhere in this pass, and not claimable: the 73 legs the mirror cannot run (68 of them
`Installers/finding-b-e2e.yml` on macOS and Windows), every deploy leg, the AWS halves of the two
Backend security legs, and every live-endpoint proof the plan marks NOT EXERCISED.

## Programme counts — 147 task headings, every one placed

| State | Count | Where |
|---|---:|---|
| MERGED | 33 | Waves 0A, 0, 1, 2, 3 (re-verified on the fetched mains), Wave −1 T3, Wave 8 T5's engineering half |
| BUILT · UNMERGED | 83 | Waves 3B–8 on the eight integration branches, plus Wave −1 T1/T2/T6/T7/T8 |
| PARTIAL | 13 | −1 T5 · 3B T2, T7 · 4A T2 (browser half), T5, T6 · 7A T5 · 7B T2, T6, T7 · 8 T3, T11, T12 |
| BLOCKED | 6 | 0A T5, T6 · 0 T1, T4 · 8 T9, T10 |
| WITHDRAWN | 2 | 4C T10, T11 |
| NOT STARTED | 3 | −1 T4 · 0 T5, T6 |
| NOT EXERCISED | 2 | 0A T1 (permanently) · 0 T7 |
| MOVED | 5 | 3 T4, T5, T11 · 5 T6, T7 |

On 2026-09-02 the same 147 read 32 MERGED, 1 BUILT · UNMERGED and roughly 100 NOT STARTED. The
merged count has moved by one because nothing has been pushed; the built count is where the three
days went.

**No wave is claimed evidence-complete.** A wave closes when its own exit and defeat set has been
run end to end. Two waves have a written closure record that measured their criteria (4C, 5); the
landing notes for 3B, 6, 7A, 7B and 7C measured what they landed; the rest have suite results and
commit-level evidence only. Per D17, 147 of 147 would still certify none of the five risk lanes:
R1, R3 and R4 stay `NOT_READY` on F16, R2 on branch protection, R5 on D16.

## Owner-controlled items, unchanged in kind and now with the count that depends on each

| Item | Rows waiting on it |
|---|---|
| Agent release authorization (combined P9 + P47 payload) | 0A T5, T6 — live customer impact continues |
| Production mutation authorization | 0 T1, T4, T7 |
| Private-key posture decision | 4A T5, T6 |
| Holdout CI trigger/cost decision | −1 T5 (trigger half), 3 T11 |
| F16 signing-key custody | 8 T10; R1/R3/R4 |
| Live canary runs on real hosts | 8 T5 (6 of 6), 8 T9 |
| One real fork-PR run before a release tag | 7A T5 — needs a push |
| Accept or reject the Static-Worker recall-baseline re-bank (`node corpus/campaign.cjs catch --write-baseline`) | 7B T8 — the repository's own guard reserves this for a human; veto-gates stay 28 of 29 until it is done |
| Port the self-vocabulary suppression to the browser twin's prompt scanner, or accept the divergence | 4A T2's browser half; Installers `pr-checks:scanner-parity` stays red until then |
| A second independent labeler and a tie-breaker (E4) | 3B T11's gate stays red; every published rate rests on one person's labels |
| Consented benign-replay data programme (E1) | 3B T7's data half; every Tier-A false-positive claim |
| Branch protection | R2 — a billing plan, not code |
| Deploy order, when the ask comes | Backend before Frontend (Frontend Task 7 derives the facet rail from a `channel` field the allowlist must already accept) and before any agent release |

Withdrawn on 2026-09-04, not blocked: red-team time (E2), Codex vendor artefacts (E3), the
independent reproducer, the KMS/HSM ceremony, CI billing. Each field they would have filled is
present and says `null`, `NOT_READY` or `[]`.

## What is still buildable without an owner decision

- Wave 8 T12 — the Installers rows of the §16.8/§16.9 defeat matrix (the Backend ledger exists).
- Wave 8 T11 — `TestSystemCardHasAPerSurfaceRow`, binding the system card's per-surface rows to code.
- Wave 8 T3 — 12 of the 13 residual sinks (S2 prompt egress stays deferred for the hook-budget reason).
- Wave 3B T2 — the `RunnerIdentity.Valid()` widening, once the D-3B-6 spine change lands upstream.
- Wave −1 T4 — the citation repair.
- Wave 0 T5/T6 — `.codefence.yml` on the seven repositories and the local-scan lane policy.

## Uncommitted or unpushed artefacts, so nobody re-discovers them

- Every integration branch above is local. Merging to any `main` and deploying each need a fresh ask.
- Root: `M47A_IMPLEMENTATION_PLAN.md` (the ATLAS edit) and this ledger are committed together;
  six earlier P47 documents were committed in `ed578cc`.
- Throwaway worktrees created for this pass and safe to delete: `/c/cwt/p47-baseline-main`,
  `/c/cwt/p47-merged-installers`, `/c/cwt/p47-baseline-backend`, and the junction directory
  `scratchpad/gateroot`. None holds work.
- `Installers/.plans/m47a-20260822/v2-waves/artifacts/sink-inventory.json` is regenerated and committed
  on the Installers integration branch.
