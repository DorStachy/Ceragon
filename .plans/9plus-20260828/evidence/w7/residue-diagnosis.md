# W7 T1 — uninstall residue diagnosis

The evidence lives in the Installers repo, not here, so the Pester guard in
`windows-installer/e2e-tests/tests/Report.Tests.ps1` can read it:

**`Installers/windows-installer/e2e-tests/evidence/residue-diagnosis.md`**

Do not copy its content here. Two copies of an evidence file is how this codebase grows
two truths.
