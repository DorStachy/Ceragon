DeVoid
Overview
Web AI
Coding AI
Autonomous AI
MCP Control Tower
Alerts
Inventory
Admin
ACCOUNT
Render QA Org
Render QA Site
ADMIN
ENDPOINTS
Search…
ctrl
K
RQ
Render QA
Admin
ADMINISTRATION
Admin
ENDPOINTS
Endpoint Management
Install & Enrollment
Fleet
Coverage
Fleet Coverage

Per-machine AI protection coverage and rollout readiness: which controls are live, by team and by endpoint. Only attested controls count; configured is not enforced.

One honest coverage truth per machine: exactly which protections are live, derived from what each endpoint actually uses. A machine is flagged only when a tool it uses is unprotected.

Governance profile truth unavailable

This deployment cannot prove which protections satisfy the selected profile, so it is not ready for external reliance.

NO COVERAGE YET
Once agents enroll and attest their controls, each machine's honest coverage appears here. Start by installing the agent on your fleet.
OPEN THE INSTALL GUIDE
Web AI Guard coverage

Control coverage, not usage. Per endpoint.

Web AI Guard coverage could not be read, so nothing here is known about which browsers are guarded. No count is shown: a zero would be indistinguishable from a fleet with no managed browsers. web coverage read returned no endpoint list

AI GOVERNANCE OPT-OUTS

An endpoint can be authorized to stop DeVoid governing a runtime. While an opt-out is in force that runtime is not governed on that endpoint, and it is not repaired automatically. Where no readable opt-out transition has been reported, the row reads “Not reported”: that coverage has not been measured.

ENDPOINTS IN SCOPE
5
OPT-OUT IN FORCE
0
OPT-OUT EXPIRED
0
NOT REPORTED
2

The opt-out coverage counts this server reported do not match the rows below, which this console resolved for itself: notReported (reported 0, rows 2). Where they differ, the count shown is the one that claims less.

2 of 5endpoints are listed below, so the rows are a subset of the scope the counts above cover. Each count is the larger of the server’s number and the rows on this screen. A count the rows exceed is reported as a disagreement; a count higher than the rows is not, because a subset cannot account for the whole scope.

DESKTOP-QA09
· codex

No readable opt-out transition has been reported here, so opt-out coverage has not been measured. This is not a statement that it is governed.

Not reported
DESKTOP-QA10
· codex

No readable opt-out transition has been reported here, so opt-out coverage has not been measured. This is not a statement that it is governed.

Not reported