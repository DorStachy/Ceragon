# Wave 1 remediation handoff — 2026-08-09

This is the continuation point for the production-verification remediation programme. Read this file first, then the implementation plan and the relevant finding spec. Do not reimplement merged work.

## Published state

- **F24 merged:** [Backend PR #240](https://github.com/Ceragon-Prod/Backend/pull/240), merge `cb5dbf9827a20ec826657720289e588cc5484679`. All 13 required checks passed.
- **F25 merged:** [Backend PR #241](https://github.com/Ceragon-Prod/Backend/pull/241), merge `1d9b5d15abcdc11a5b9c8e46bfe30f75bbe3a28f`. Required checks passed, including 15/15 disposable live-Postgres tests.
- **Seven source-of-truth rewrites merged:** [docs PR #145](https://github.com/Ceragon-Prod/docs/pull/145), merge `bb7a349b8dbddaa0c2015fdf93d1395ea250a026`.
- **Wave 1 progress:** 2 of 13 work packages merged; 11 remain.

No application deployment or post-merge production rollout verification was performed. The SOT refresh did include read-only AWS inventory inspection; that dated snapshot is not a claim that merged application code has rolled forward.

## What F24 implemented

- The Backend clamps requested delegated-approval expiry to `serverNow + 900s`.
- Shorter requested windows remain shorter; invalid or past expiries still fail.
- A clamp emits bounded WARN telemetry with org, endpoint, and delta.
- Expiry is no longer part of action identity for idempotent recreation; every prepared-action binding remains enforced.
- Only endpoint create/consume body DTOs are agent-wire tolerant. Human presence and completion DTOs remain strict.
- The route inventory recognizes all five raw-guard delegated-approval endpoint handlers and exactly the two endpoint body DTOs.

F24 residuals:

- An endpoint clock more than 900 seconds behind can still send an already-expired instant and receive 400.
- Duration-based `ttlSeconds` belongs to Wave 3.
- The agent still keeps its local deadline. Margin and adoption of the echoed server expiry belong to Wave 4.

## What F25 implemented

- Shared per-item validation for AI-context and legacy evidence batches.
- Batch envelopes and AI-context coverage remain strict.
- Valid object-shaped siblings survive malformed object-shaped items. Primitive/null items and oversize arrays remain batch-fatal envelope errors.
- Legacy evidence rejection persists a content-free tombstone at the original stream/sequence slot in the same transaction as accepted events.
- Real sequence gaps remain gaps; the signed contiguous floor never skips them.
- `rejectedEventIds` is additive and intentionally outside the existing ACK-signature transcript.
- Global Nest validation behavior is unchanged. Do **not** set `forbidUnknownValues` on `AgentIngestValidationPipe`; direct per-item options remain separate.

F25 residuals:

- Installed agents ignore `rejectedEventIds`.
- Mixed batches can advance accepted items, but poisoned WAL entries remain.
- Rejected-only ACKs still fail old-agent validation.
- Wave 4 must add producer clamping and coverage retry, rejected-only ACK handling, accepted-plus-rejected WAL deletion only after signed-floor verification, and durable rejection health metrics.
- Never replace rejection tombstones with deletion, classify a tombstone as accepted, or advance across a missing sequence.

## Remaining Wave 1 order

Safe independent sequence:

1. **F12 — build identity.**
2. **F19 — retention truth.**
3. **F13 — installer artifact/signature resolution.**
4. **F17 — verified endpoint attribution.** Its F25 dependency is satisfied.

Then coordinate before shared files:

5. **M2 — F27 + F32 backend predicate.** It must land before Design T-L13.
6. **F26 — after M2, in the same deploy.**
7. **F14(a+c) — settle manifest semantics/scope and runtime task-role IAM first.**
8. **F10 — re-derive before coding; the written AWS mechanism was invalid.**
9. **F11 — planner decision required before implementation.**
10. **F36 Stage 1 — Design owns the frontend render; remediation must not duplicate it.**
11. **F41 — coordinate with F8b as one constants edit before Design T-E2/T-U4/T-U9.**

Cross-wave gates:

- M2 must land before Design T-L13.
- M2/F27 must precede F26, F28, F31, and F32.
- F8b + F41 must be one coordinated edit.
- F36 Stage 1 belongs to Design Phase U/T-E13.
- Generated security types are digest-pinned. Regenerate them; never hand-edit them.

## Corrections that override older prose

### F10 AWS API correction

`ApproximateAgeOfOldestMessage` is a CloudWatch SQS metric, not an SQS `GetQueueAttributes` attribute. Re-design the reader and IAM before implementation. Depth can come from SQS; age requires CloudWatch `GetMetricData`/`GetMetricStatistics` or another proven source with explicit lag, freshness, cache, and `NOT_MEASURED` semantics.

Use the official [GetQueueAttributes API](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_GetQueueAttributes.html) and [SQS CloudWatch metrics](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-available-cloudwatch-metrics.html). F10 also needs a durable lane discriminator, broader reaper-state reasoning, and measured normal drain time before selecting a refusal threshold.

### Other binding corrections

- **F11:** `READ-THIS-FIRST.md` says inspect/restate rather than code-fix, while the detailed spec prescribes durable columns and metrics. Stop for a planner decision; do not guess.
- **F14:** the Backend ECS task role needs `s3:GetObject`; deploy-role permission alone changes nothing.
- **F13:** repair sorted-key compact-manifest signature verification before making that manifest authoritative.
- **F17:** use `verifiedAgentId ?? 'unattributed:' + apiKeyId`; do not ship the older `UnauthorizedException`. Batch any cleanup migration.
- **F19:** two of three original claims were wrong. Audit and delivery retention already run. Add an explicit-warning surface and remove only the threat-intel gate; do not add a boot assertion.

## Authoritative reading order

1. [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md)
2. [`fix-specs/READ-THIS-FIRST.md`](fix-specs/READ-THIS-FIRST.md)
3. The finding spec for the work being picked up
4. [`PRODUCTION_VERIFICATION_CHECKLIST_20260808.md`](PRODUCTION_VERIFICATION_CHECKLIST_20260808.md) when a live claim is in scope

Useful finding specs:

- [`fix-specs/DELIVERY.md`](fix-specs/DELIVERY.md) — F24/F25 history and residuals
- [`fix-specs/BACKENDOPS.md`](fix-specs/BACKENDOPS.md) — F12, F19, F13, F17, F14, F10, F11
- [`fix-specs/SESSIONS.md`](fix-specs/SESSIONS.md) — M2 and F26
- [`fix-specs/FRONTEND.md`](fix-specs/FRONTEND.md) — F36
- [`fix-specs/DEFAULTS.md`](fix-specs/DEFAULTS.md) — F41

The concurrent Design plan was local at `<WORKSPACE>\.design-mockups\v2\IMPLEMENTATION-PLAN.md`. It is not part of this publication package. Obtain the current Design plan before crossing any Design coordination gate.

## Repositories to fetch

| Area | Repository | Base branch |
|---|---|---|
| Plan and handoff | `DorStachy/Ceragon` | `master` |
| Backend | `Ceragon-Prod/Backend` | `main` |
| Frontend | `Ceragon-Prod/Frontend` | `main` |
| Installers | `Ceragon-Prod/Installers` | `main` |
| Intelligence | `Ceragon-Prod/Ceragon-Intelligence` | `main` |
| Source-of-truth docs | `Ceragon-Prod/docs` | `main` |

The refreshed SOTs are under `MostUpdated_SourceOfTruth/` in the docs repository:

- `Installer_SOURCE_OF_TRUTH.md`
- `AI_CONTROL_PLANE_SOURCE_OF_TRUTH.md`
- `AWS_INFRASTRUCTURE_SOURCE_OF_TRUTH.md`
- `DATA_FLOWS_SOURCE_OF_TRUTH.md`
- `Code_Security_SOURCE_OF_TRUTH.md`
- `SUPPLY_CHAIN_SOURCE_OF_TRUTH.md`
- `MCP_IDE_EXTENSION_PROTECTION_SOURCE_OF_TRUTH.md`

## Safe continuation workflow

1. Confirm GitHub authentication.
2. Fetch the current base branch without modifying a dirty shared checkout.
3. Create a fresh worktree from `origin/main` for exactly one finding.
4. Read `AGENTS.md`, the full plan, `READ-THIS-FIRST.md`, and the finding's adversarial-review section.
5. Use CodeGraph first for structural questions.
6. Work test-first: record an observed-red regression or defeat test, implement the smallest mechanism, then rerun focused green tests.
7. Run the relevant build/lint gate. For database behavior, use a disposable isolated Postgres and run the gated live-PG suite.
8. Review the fixed-point diff against the exact base; stage explicit files only.
9. Push one branch and open one PR per finding. Merge only after required checks and review are green.
10. Update this handoff/checkpoint after each merged package.

Example start for F12:

```powershell
gh auth status
git -C "<WORKSPACE>\Backend" fetch origin main
git -C "<WORKSPACE>\Backend" worktree add `
  "<SHORT_WORKTREE_ROOT>\rem-f12" `
  -b fix/rem-f12-build-sha origin/main
```

Guardrails:

- Never work from dirty shared component checkouts.
- Never branch from a stale local branch; fork from current `origin/main`.
- Never use `git add -A` in the shared workspace.
- Do not run `npm install` or `npm ci` inside a worktree; follow the repository's existing dependency-junction instructions.
- Preserve unrelated user changes and generated artifacts.
- Do not modify the Nest global unknown-value policy.
- Do not alter F25 tombstone/contiguity semantics.
- Treat AWS work as read-only unless a separately approved ops step explicitly authorizes mutation.

## Private evidence boundary

The three raw Codex transcripts and the live endpoint evidence ledger remain in the authorized local workspace. They were secret-scanned; no credential was detected, but they contain machine identifiers and personal paths, so they are intentionally not published. The derived conclusions required for implementation are in the plan and fix specs. Do not publish the raw artifacts without a fresh privacy review.

## Suggested skills

- `diagnose`
- `tdd`
- `review`
- `supabase-postgres-best-practices`
- `github:gh-address-comments`
- `github:yeet`
- `handoff`

The ready-to-paste continuation prompt is [`COWORKER_FRESH_CHAT_PROMPT.md`](COWORKER_FRESH_CHAT_PROMPT.md).
