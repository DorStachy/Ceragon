{
  "name": "@ceragon/shared-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared type contracts for Ceragon package intelligence (Ingestion + Backend)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "npm run build && node scripts/test-security-taxonomy.cjs && node scripts/test-vulnerability-applicability.cjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
